{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "clarkt-com",
  "compatibility_date": "2026-08-26",
  // No "main": the site is fully static, so Cloudflare serves these files
  // directly and no Worker is invoked per request.
  "assets": {
    "directory": "./dist"
  }
}
